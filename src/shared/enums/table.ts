export enum EGenTable {
    NAME = 'name',
    HASH = 'hash',
    SEED = 'seed',
    KEY = 'key',
    QUANTITY = 'quantity',
}
export enum HashType {
    SHA1 = 'sha1',
    SHA256 = 'sha256',
    SHA3 = 'sha3'
}
